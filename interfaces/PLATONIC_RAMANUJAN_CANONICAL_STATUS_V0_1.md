# Platonic–Ramanujan Cross-Repository Canonical Status v0.1

Status: `CANONICAL_STRUCTURAL_INTERFACE / RUNTIME_ACTIVATION_REJECTED_V0_1 / GRAPH_REGISTRY_SYNC_PENDING`

Date: 2026-09-11

The TIR finite spectral carrier `G36 = I12 square K3` and the PNCS disabled structural implementation are admitted as a cross-repository structural interface.

Canonical scope:

```text
TIR Platonic L constants -> finite Platonic/Ramanujan spectral carrier
PNCS ordered 12x3 lane structure -> G36 structural support
G36 exact spectrum/gap -> disabled PNCS spectral operator API
isolated Lyapunov identity -> exact mathematical corollary
```

Runtime scope:

```text
additive C_PR in parent v0.32 = REJECTED_V0_1
support-mask activation on parent adaptive g = OPEN_SEPARATE_GATE
orbital Wigner/Gaunt equivalence = OPEN
physical binding = OPEN
```

The fresh comparative receipt showed lower disagreement energy but worse paired perturbation recovery for every tested positive beta, so no runtime-stability edge is promoted.

This status file is canonical provenance. `dependency_graph.yaml` and `claims.jsonl` remain to be synchronized through their own deterministic validator before this interface is represented as a canonical graph-registry edge. Until that sync occurs, downstream IDT/RFC/Standard-Model/GR/cosmology claims are unchanged.